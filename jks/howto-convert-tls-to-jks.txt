You have a typical OpenSSL self-signed setup:

server.key   # private key
server.crt   # certificate
server.csr   # signing request (not needed anymore)


To use these in Java (JKS), you must create:

✅ Keystore → contains private key + cert

✅ Truststore → contains trusted certificate(s)








server.key + server.crt
        ↓
     PKCS12 (.p12)
        ↓
     JKS keystore






server.crt
        ↓
     JKS truststore




1️⃣ Create a PKCS12 file (required intermediate step)

Java cannot import PEM keys directly, so we first create a .p12.

openssl pkcs12 -export \
  -in server.crt \
  -inkey server.key \
  -name server \
  -out server.p12




2️⃣ Convert PKCS12 → JKS Keystore

keytool -importkeystore \
  -destkeystore server.keystore.jks \
  -deststoretype JKS \
  -deststorepass changeit \
  -srckeystore server.p12 \
  -srcstoretype PKCS12 \
  -srcstorepass <p12-password> \
  -alias server


📌 Result:

server.keystore.jks


This keystore contains:

✔ Private key

✔ Certificate





3️⃣ Create a JKS Truststore (from server.crt)

Truststore contains only certificates, no private keys.

keytool -import \
  -trustcacerts \
  -alias server \
  -file server.crt \
  -keystore server.truststore.jks \
  -storepass changeit





4️⃣ Verify keystore & truststore

✅ Keystore



keytool -list -v \
  -keystore server.keystore.jks \
  -storepass changeit


Should show:

Entry type: PrivateKeyEntry





✅ Truststore


keytool -list -v \
  -keystore server.truststore.jks \
  -storepass changeit


Should show:

Entry type: trustedCertEntry






5️⃣ Typical Java SSL configuration
Keystore (server side)
-Djavax.net.ssl.keyStore=server.keystore.jks
-Djavax.net.ssl.keyStorePassword=changeit

Truststore (client side)
-Djavax.net.ssl.trustStore=server.truststore.jks
-Djavax.net.ssl.trustStorePassword=changeit
