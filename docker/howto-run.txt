# Build the image
docker build -f Dockerfile-minimal -t myubuntu:latest .

# Run it interactively
docker run -it myubuntu

# Test inside the container
ping -c2 8.8.8.8
python3 --version
git --version
