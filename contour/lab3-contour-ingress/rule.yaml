apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-kubia
  labels:
    app: my-kubia
spec:
  defaultBackend:
    service:
      name: kubia-clusterip
      port:
        number: 8080
