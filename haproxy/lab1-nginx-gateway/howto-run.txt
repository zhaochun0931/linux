A Gateway is used to manage all inbound requests, and is a key Gateway API resource.

When a Gateway is attached to a GatewayClass associated with NGINX Gateway Fabric, it creates a Service and an NGINX deployment in the same namespace as the Gateway. This forms the NGINX data plane, handling requests.

A single GatewayClass can have multiple Gateways: each Gateway will create a separate Service and NGINX deployment.














kubectl apply -f tea.yaml
kubectl apply -f cafe.yaml



kubectl apply -f gateway.yaml
kubectl apply -f http-route.yaml




kubectl describe gateway




kubectl get svc


$ kubectl get svc
NAME               TYPE           CLUSTER-IP       EXTERNAL-IP   PORT(S)        AGE
coffee             ClusterIP      10.109.232.12    <none>        8080/TCP       26m
kubernetes         ClusterIP      10.96.0.1        <none>        443/TCP        33m
my-gateway-nginx   LoadBalancer   10.110.240.110   localhost     80:31376/TCP   21m
tea                ClusterIP      10.102.175.247   <none>        8080/TCP       26m
$

curl -i -H "Host: cafe.example.com" http://localhost/tea


curl -i -H "Host: cafe.example.com" http://localhost/coffee

